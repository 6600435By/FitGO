# Go / No-go — готовность к Фазе B (FitGO)

Заполняется после `.\probe-all.ps1`.

## Обязательные тесты

- [ ] **R1** `forma` @ :444 `/classes/` → HTTP 200 (регресс)
- [ ] **R2** `fitgo` @ :445 `/api/v3/classes/` → HTTP 200
- [ ] **R3** `fitgo` @ :445 `auth_client` → HTTP 200, есть `user_token`
- [ ] **R4** `fitgo` @ :445 `/fitgo/v1/health` → HTTP 200
- [ ] **R5** `fitgo` @ :445 `/fitgo/v1/client?phone=` → HTTP 200, корректное ФИО
- [ ] **R6** `fitgo` @ :445 `/fitgo/v1/membership?phone=` → HTTP 200

## Безопасность

- [ ] Публикация `forma` не изменялась
- [ ] Расширение `.cfe`, не правки типовой
- [ ] Секреты только в `.env.probe`
- [ ] План отката Apache/1С понятен

## Решение

| Поле | Значение |
|------|----------|
| Дата | |
| Исполнитель | |
| **Go / No-go** | ДА / НЕТ |
| Комментарий | |

**Фаза B (интеграция в FitGO)** — только при **Go = ДА** и заполненном [report.md](../report.md).
